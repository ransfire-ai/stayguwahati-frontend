'use client'; import {useState} from 'react'; export default function PropertyImage({src,alt,priority=false}:{src?:string;alt:string;priority?:boolean}){const [failed,setFailed]=useState(false);return <div className="sg-property-image">{src&&!failed?<img src={src.replace('/upload/','/upload/f_auto,q_auto,w_900/')} alt={alt} loading={priority?'eager':'lazy'} fetchPriority={priority?'high':'auto'} onError={()=>setFailed(true)}/>:<div style={{padding:20,color:'#66727B'}}>StayGuwahati photo</div>}</div>}
