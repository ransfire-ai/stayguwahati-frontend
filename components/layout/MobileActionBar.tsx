'use client';
import React from 'react';
export default function MobileActionBar({children}:{children:React.ReactNode}){return <div className="sg-mobile-action"><div className="sg-container">{children}</div></div>}
